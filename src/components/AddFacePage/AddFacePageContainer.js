import { connect } from 'react-redux'
import AddFacePage from './AddFacePage'

const mapStateToProps = (state) => ({

})

const mapDispatchToProps = (dispatch) => ({

})

const AddFacePageContainer = connect(mapStateToProps, mapDispatchToProps)(AddFacePage)

export default AddFacePageContainer
